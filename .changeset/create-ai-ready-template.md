---
'create-svelte-docsmith': minor
---

Scaffold AI-ready, SEO-ready projects. The template now ships a `sitemap.xml` and `robots.txt`, `llms.txt` / `llms-full.txt` endpoints, a catch-all `[...slug].md` route for per-page Markdown, and enables the `copyPage` "Copy page" button on `DocsShell`. The `site-config.ts` gains commented `url` and `editUrl` hints, and the template pins `svelte-docsmith@^0.5.1`.
